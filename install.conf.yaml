- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.config: config

- shell:
  - source install-doom.fish && install-doom
  - curl https://git.io/fisher --create-dirs -sLo ~/.config/fish/functions/fisher.fish && fisher
